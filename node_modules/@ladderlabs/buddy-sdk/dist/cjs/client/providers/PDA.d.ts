import { Program } from "@project-serum/anchor";
import { Buddylink } from "../../idl/buddylink";
import { PublicKey } from "@solana/web3.js";
export declare class PDA {
    private program;
    constructor(program: Program<Buddylink>);
    getMasterTreasuryPDA(mint: PublicKey): PublicKey;
    getAmbassadorPDA(name: string): PublicKey;
    getOrganizationPDA(name: string): PublicKey;
    getMemberPDA(organizationName: string, name: string): PublicKey;
    getMemberStatisticPDA(organizationName: string, memberPDA: PublicKey): PublicKey;
    getBuddyPDA(name: string): PublicKey;
    getProfilePDA(name: string): PublicKey;
    getTreasuryPDA(owners: PublicKey[], shares: number[], mint?: PublicKey): PublicKey;
    getSimpleTreasuryPDA(buddyPDA: PublicKey, mint?: PublicKey): PublicKey;
}
